(b) documentar pré-requisitos, variáveis de ambiente, comandos de build, comandos de
execução, processo de deploy e forma de validação do ambiente;
(c) indicar como reproduzir o ambiente localmente, quando aplicável.